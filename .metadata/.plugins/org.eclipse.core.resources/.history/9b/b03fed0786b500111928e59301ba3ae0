server:
  port: 8084

spring:
  application:
    name: fine-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/bibliotech_fines?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME:bibliotech}
    password: ${SPRING_DATASOURCE_PASSWORD:bibliotech}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

bibliotech:
  jwt:
    secret: BibliotechSecretKeyForJwtAuthenticationVeryLongSecureKey2026!
  fine:
    daily-rate: 10.00

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
